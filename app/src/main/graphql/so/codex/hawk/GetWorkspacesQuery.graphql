query workspaces($ids: [ID] = []) {
    workspaces(ids: $ids) {
        id,
        balance,
        description,
        name,
        projects {
            id,
            name,
            unreadCount,
            description
        }
    }
}